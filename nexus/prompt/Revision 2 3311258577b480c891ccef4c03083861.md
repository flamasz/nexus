# Revision 2

1. When a new order date is selected from the date picker, it doesn't show the new date in the order block. It should show the updated date after clicking a new date.
2. The item name should not be in a pill. it should just look like text, like the version field, but make the text bold.
3. Make the order block container match the full width of the viewport with some padding.
4. make the item name column 20% less wide horizontally.
5. Add a Priority column that functions like the approval status field, these are the options: 1 - Critical, 2 - Standard, 3 - Low. Make new items automatically set to 2 - Standard.
6. Move the columns around to match this sequence from left to right; Priority, Order Status,  Item Name, Category, Qty, version, Approval, Art, Notes